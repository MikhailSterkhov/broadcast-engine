<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.github.mikhailsterkhov</groupId>
        <artifactId>social-broadcast-parent</artifactId>
        <version>1.0.4</version>
    </parent>

    <artifactId>social-broadcast-examples</artifactId>

    <properties>
        <h2database.version>2.1.214</h2database.version>
        <jedis.version>5.1.5</jedis.version>
        <spigot.version>1.12.2-R0.1-SNAPSHOT</spigot.version>

        <maven.compiler.source>8</maven.compiler.source>
        <maven.compiler.target>8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-engine</artifactId>
            <version>1.0.4</version>
        </dependency>

        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-hibernate</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-spring-data</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-redis</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-jdbc</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-smtp</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-telegram</artifactId>
            <version>1.0.4</version>
        </dependency>
        <dependency>
            <groupId>com.github.mikhailsterkhov</groupId>
            <artifactId>social-broadcast-spigot</artifactId>
            <version>1.0.4</version>
        </dependency>

        <!-- Other helpful dependencies -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2database.version}</version>
        </dependency>
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
            <version>${jedis.version}</version>
        </dependency>
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot</artifactId>
            <version>${spigot.version}</version>
        </dependency>
    </dependencies>
</project>